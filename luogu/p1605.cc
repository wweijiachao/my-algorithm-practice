#include <cstdio>
#include <iostream>
#include <vector>

using namespace std;

class Solution {
    ;
};
int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    #ifdef LOCAL
        freopen("input.txt", "r", stdin);
    #endif

    return 0;
}